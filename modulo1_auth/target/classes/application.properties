spring.application.name=main

# --- Configura��es do Supabase (Transaction Pooler) ---
# Note a porta 6543 e o host 'aws-1...' que apareceu na sua imagem
spring.datasource.url=jdbc:postgresql://aws-1-sa-east-1.pooler.supabase.com:6543/postgres?sslmode=require

# O USU�RIO MUDOU! No pooler, ele � longo assim mesmo:
spring.datasource.username=postgres.pglfamjhvxpzwwxsuojz

# Coloque sua senha real aqui
spring.datasource.password=81496365alfa

spring.datasource.driver-class-name=org.postgresql.Driver

# --- TRUQUE PARA O TRANSACTION POOLER (IMPORTANTE) ---
# Como o pooler n�o suporta prepared statements, desativamos isso no driver:
spring.datasource.hikari.data-source-properties.prepareThreshold=0

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Desativa sess�o aberta na view (boa pr�tica com poolers)
spring.jpa.open-in-view=false

api.security.token.secret=${JWT_SECRET:my-secret-key}